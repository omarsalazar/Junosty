from django.conf.urls import url
# from .views import Horario, Materias
#
# urlpatterns = [
#     url(r'^horario/', Horario.as_view(), name='horario'),
#     url(r'^materias/', Horario.as_view(), name='materias'),
# ]
